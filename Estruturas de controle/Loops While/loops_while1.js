// Inicializa a variável de controle
let contador = 1;

// O laço executa enquanto o contador for menor ou igual a 5
while (contador <= 5) {
    // Imprime o valor atual no console
    console.log("Número: " + contador);
    
    // Incrementa a variável para evitar um loop infinito
    contador++;
}
